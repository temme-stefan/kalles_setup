'use strict';
addEventListener("DOMContentLoaded", () => {
    const output = document.getElementById('outputId');
    if (output != null) {
        output.value = "Dieser Text kommt aus JS."
    }
})